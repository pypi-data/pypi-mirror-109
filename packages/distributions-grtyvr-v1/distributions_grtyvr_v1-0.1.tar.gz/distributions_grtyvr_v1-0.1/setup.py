from setuptools import setup

setup(name='distributions_grtyvr_v1',
      version='0.1',
      description='Statistical distributions',
      packages=['distributions_grtyvr_v1'],
      zip_safe=False)
