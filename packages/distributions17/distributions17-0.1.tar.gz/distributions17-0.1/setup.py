from setuptools import setup

setup(name='distributions17',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['distributions17'],
      author = 'Andrew Alarcon',
      zip_safe=False)
