from setuptools import setup

setup(name='distributions18',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['distributions18'],
      author = 'Andrew Alarcon',
      zip_safe=False)
